[package]
name = "nvr-dashboard"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
axum = { workspace = true }
rust-embed = { workspace = true, features = ["mime-guess"] }

[dev-dependencies]
tokio = { workspace = true }
tower = { workspace = true, features = ["util"] }
